{% include 'Top10CabinetBundle:Form:register.html.twig' %}

{#<form class="ym-form login" action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST">
    {{ form_widget(form) }}
    
    <div class="ym-fbox-button">
    	<input type="submit" class="ym-button" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
    
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>#}
